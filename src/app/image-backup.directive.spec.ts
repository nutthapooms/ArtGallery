import { ImageBackupDirective } from './image-backup.directive';

describe('ImageBackupDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageBackupDirective();
    expect(directive).toBeTruthy();
  });
});
